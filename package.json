{
  "name": "project",
  "version": "1.0.0",
  "scripts": {
    "dev": "serve . -l 5173",
    "build": "mkdir -p dist && cp index.html dist/ && cp -r images dist/"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
